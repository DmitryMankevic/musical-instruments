export enum CLIENT_ROUTES {
  HOME = "/",
  LOGIN = "/login",
  SIGN_UP = "/signup",
  PROFILE = "/profile",
  FAVOURITE = "/favourite",
  ITEM_CARD = "/item-card",
  ITEMS = "/items",
  CART = "/cart",
  ORDER = "/order"
}
