export enum CLIENT_ROUTES {
  HOME = "/",
  LOGIN = "/login",
  SIGN_UP = "/signup",
  ADMIN = "/admin",
  PROFILE = "/profile",
  FAVOURITE = "/favourites",
  ITEM_CARD = "/item-card",
  ITEMS = "/items",
  CART = "/cart",
  ORDER = "/orders",
  CATEGORY = "/category/:id",
  CATALOG = "/catalog",
}
